services:
  regcytes.repository:
    class: Drupal\regcytes\GroupPathPrefixRepository
    arguments:
      - '@database'
      - '@entity_type.manager'
      - '@entity_field.manager'
      - '@entity.repository'
      - '@request_stack'
  regcytes.path_prefix_context:
    class: Drupal\regcytes\Context\GroupContextPathPrefix
    arguments:
      - '@request_stack'
      - '@current_route_match'
      - '@regcytes.repository'
    tags:
      - { name: 'context_provider' }
  regcytes.site_prefix_manager:
    class: Drupal\regcytes\SitePrefixManager
    arguments:
      - '@regcytes.repository'
      - '@config.factory'
  regcytes.path_processor:
    class: Drupal\regcytes\PathProcessor\GroupPathPrefixProcessor
    arguments:
      - '@regcytes.repository'
      - '@regcytes.site_prefix_manager'
      - '@config.factory'
      - '@entity_type.manager'
  regcytes.route_subscriber:
    class: Drupal\regcytes\Routing\GroupHomepageRouteSubscriber
    tags:
      - { name: event_subscriber }

